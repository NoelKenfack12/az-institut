<div class="row">
<div class="col-md-8 col-md-offset-2 col-sm-12">
	{{ form_start(form, {'attr': {'class':'row', 'style': 'margin-top: 15px', 'action': path('users_adminuser_modification_categorie_produit', {'id': cat.id }) }}) }}
	
		 <div class="input-field col s12">
			{{ form_widget(form.nom) }}
			{{ form_label(form.nom,'Nom de la formation') }}
		 </div>
		 
		 <div class="input-field col s12">
			{{ form_widget(form.rang) }}
			{{ form_label(form.rang,'Numéro dans le classement') }}
		 </div>
		 
		 <div class="input-field col s12">
			{{ form_widget(form.description) }}
			{{ form_label(form.description,'Description de la formation') }}
		  </div>
		  
		  <div class="input-field col s12">
			  <select name="typeservice" class="browser-default">
				<option value="continue" {% if cat.typeservice == 'continue' %}selected{% endif %}>Type de formation</option>
				<option value="continue" {% if cat.typeservice == 'continue' %}selected{% endif %}>Formation continue</option>
				<option value="diplomante" {% if cat.typeservice == 'diplomante' %}selected{% endif %}>Formation diplomante</option>
			  </select>
		  </div>
	  
		 <div class="input-field col s12">
			{{ form_widget(form.file, {'attr': {'class': 'form-control', 'style': 'opacity: 1, color: #333;'}}) }}
		 </div>

	  <div class="input-field col s12">
		  <div class="form-group" style="margin-top: 15px;">
			<button class="btn btn-primary btn-lg">Enregistrez</button>
		  </div>
	  </div>
	 <div class="clearfix"></div>
	 {{ form_end(form) }}
	<div class="clearfix"></div>
</div>
</div>
<script type="text/javascript" src="{{ asset('admintemplate/js/materialize.js') }}"></script>