<div class="row" style="border-top: 3px solid #333; margin-top: 15px; padding-top: 5px; padding-bottom: 10px; background: #f7f7f7;">
<div class="col-md-3">
  <img src="{% if app.user != null and app.user.imgprofil != null %}{{ asset(app.user.imgprofil.getwebpath) }}{% else %}{{ asset('template/images/projets/afrique.png') }}{% endif %}" alt="{{ app.user.name(25) }}" width="50" height="50" style="float: right;">  
</div>

<div class="col-md-9">
<div><strong>{{ app.user.name(25) }}</strong></div>
  <form method="post" action="{{ path('users_user_update_reponse_billet', {'id': reponse.id, 'position': position}) }}">
  <div class="form-group input-field" method="post" action="">
	<textarea name="reponse" class="materialize-textarea form-control" id="reponsebillet-{{ reponse.id }}" style="width: 90%; background: #fff;" required="">{{ reponse.description }}</textarea>
	<label for="reponsebillet-{{ reponse.id }}">Ajouter une réponse</label></br>
  </div>
  <div class="form-group  input-field">
	  <button class="btn btn-primary cyan waves-effect waves-light pull-left" type="submit" name="action">Enregister
		<i class="mdi-content-send right"></i>
	  </button>
  </div>
  </form>
</div>
</div>